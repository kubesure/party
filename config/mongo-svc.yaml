apiVersion: v1
kind: Service
metadata:
  labels:
    app: mongo-party
    role: master
    tier: backend
  name: mongopartysvc
spec:
  ports:
  - port: 27017
    protocol: TCP
    targetPort: 27017
  selector:
    app: mongo-party
    role: master
    tier: backend
