apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: party
    role: business
    type: backend
  name: party
spec:
  replicas: 2
  selector:
    matchLabels:
      app: party
      role: business
      type: backend
  template:
    metadata:
      labels:
        app: party
        role: business
        type: backend
    spec:
      containers:
      - image: party:v1
        name: party
        ports:
        - containerPort: 50051